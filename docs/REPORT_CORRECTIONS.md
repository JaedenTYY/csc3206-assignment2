# Report Corrections

The following manual corrections must be applied to the final report PDF or its original source document to align with the final verified codebase.

## Section 2.1
- **Existing issue:** May falsely claim that sparse graphs are fully handled without providing actual test evidence or describing how unavailable edges are explicitly skipped.
- **Exact replacement paragraph:** "The graph model supports asymmetric directed costs and explicitly handles sparse or disconnected graphs. To ensure robustness, the algorithm skips unavailable edges during traversal, avoiding uncaught errors when computing costs for absent edges. This has been thoroughly verified through explicit tests simulating missing edges and disconnected nodes."
- **Manual action still required:** Update the text in the final report document.

## Section 4.3
- **Existing issue:** May reference the old test coverage numbers or branch coverage incorrectly.
- **Exact replacement paragraph:** "The codebase is validated by a comprehensive automated test suite. The final verified test suite contains 26 passing tests and achieves 99% statement coverage with branch tracking enabled, ensuring both functional correctness and robust edge-case handling."
- **Manual action still required:** Update the text in the final report document.

## Section 5.2
- **Existing issue:** May describe `get_neighbours` or the main algorithm loop without mentioning the `has_edge` check.
- **Exact replacement paragraph:** "During traversal, algorithms invoke `get_neighbours(location)` to generate successors. To safely handle sparse matrices, each neighbor is subsequently verified using `has_edge(location, neighbour, metric)` before evaluating its exact transition cost via `get_cost()`. This explicitly prevents unexpected errors during execution."
- **Manual action still required:** Update the text in the final report document.

## Section 8.3
- **Existing issue:** Visualization output filenames might still be referenced as a static `route_output.png`.
- **Exact replacement paragraph:** "Visualizations generated by the system now dynamically reflect the optimization metric. Instead of a single static image, the outputs are saved as `assets/route_output_distance.png`, `assets/route_output_time.png`, or `assets/route_output_carbon.png`, allowing results from different metrics to coexist without overwriting each other."
- **Manual action still required:** Update the text in the final report document.

## Section 9.2
- **Existing issue:** Windows console encoding issues related to special characters (e.g., CO₂, box drawing) may be undocumented.
- **Exact replacement paragraph:** "To ensure cross-platform compatibility, particularly on Windows environments, the command-line interface explicitly reconfigures `sys.stdout` to use UTF-8 encoding. This guarantees that special Unicode characters, such as the CO₂ subscript and box-drawing elements, render correctly without causing encoding exceptions."
- **Manual action still required:** Update the text in the final report document.

## Section 10
- **Existing issue:** The task allocation description may conflict with actual implementation ownership in `docs/TASKS.md` or the `README.md`.
- **Exact replacement paragraph:** "Project responsibilities evolved dynamically. While `docs/TASKS.md` records the initial assignment and allocation of tasks, practical responsibilities shifted during the final integration phase. The final ownership and contribution descriptions are accurately reflected in the final README."
- **Manual action still required:** Update the text in the final report document.

## Appendix B
- **Existing issue:** CLI evidence might show outdated commands, lack UTF-8 encoding fixes, or not cover the new metric-specific execution.
- **Exact replacement paragraph:** "Figure B.1: CLI execution output demonstrating exact state-expansion counts and UTF-8 encoding for distance, time, and carbon metrics."
- **Figure/table caption change:** "Figure B.1: CLI execution output demonstrating exact state-expansion counts and UTF-8 encoding for distance, time, and carbon metrics."
- **Manual action still required:** Replace the CLI execution screenshots in the report with new ones generated from the updated codebase.

## Appendix C
- **Existing issue:** Visualizations may be named incorrectly or overwrite each other.
- **Exact replacement paragraph:** "Figure C.1: Network visualizations for optimal routes saved as `assets/route_output_distance.png`, `assets/route_output_time.png`, and `assets/route_output_carbon.png`."
- **Figure/table caption change:** "Figure C.1: Network visualizations for optimal routes saved as `assets/route_output_distance.png`, `assets/route_output_time.png`, and `assets/route_output_carbon.png`."
- **Manual action still required:** Update the visualization screenshots and ensure captions reference the correct dynamic filenames.

## Tables 2–4
- **Existing issue:** The stated routes, costs, and expansion counts must perfectly match the final execution.
- **Exact replacement paragraph:** 
  - Distance optimum: 44.6000 km | A* expansions: 11 | UCS expansions: 132 | GBFS expansions: 7
  - Time optimum: 79.0000 min | A* expansions: 20 | UCS expansions: 163 | GBFS expansions: 7
  - Carbon optimum: 7.2574 kg CO₂e | A* expansions: 11 | UCS expansions: 132 | GBFS expansions: 7
- **Figure/table caption change:** "Tables 2-4: Final algorithm metrics and expansion counts for Distance, Time, and Carbon optimization."
- **Manual action still required:** Verify and edit the values in Tables 2, 3, and 4 to strictly match these metrics.
